grow_version: ">=0.2.5"

title: "Instituto De Oncología Intervencionista"

home: /content/pages/home.yaml

localization:
  default_locale: es

meta:
  google_analytics_id: ""
  google_site_verification: ""
  image_path: ""
  favicon_path: /static/images/logo_white_transparent.png
  logo_path: /static/images/logo.png
  social:
    youtube: UChW_-RvPlRiQuIx0L-rVYcg
    facebook: idoimexico
    instagram: idoimexico

preprocessors:
- kind: gulp
  build_task: grow-build

static_dirs:
- static_dir: /static/
  serve_at: /static/
- static_dir: /dist/css/
  serve_at: /css/
  fingerprinted: true

sitemap:
  enabled: true

deployments:
  default:
    destination: local
    out_dir: /home/darival/Code/idoiDeploy